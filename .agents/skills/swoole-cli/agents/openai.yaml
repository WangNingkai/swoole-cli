interface:
  display_name: "Swoole Cli"
  short_description: "Repo-specific patterns and workflows for swoole-cli"
  default_prompt: "Use the swoole-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true